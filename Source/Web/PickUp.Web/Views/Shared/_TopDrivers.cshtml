@model IEnumerable<PickUp.Web.ViewModels.Users.DriverViewModel>
@{ 
    var index = 0;
}
<div class="panel panel-default">
    <div class="panel-heading text-center"><h4>Top 10 drivers</h4></div>
    <div class="panel-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>User</th>
                    <th>Rating</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var driver in @Model)
                {
                    var className = "";

                    if (index % 2 != 0)
                    {
                        className = "success";
                    }
                    index += 1;
                    <tr class="@className">
                        <td>@driver.UserName</td>
                        <td>@driver.Rating</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


